name: voice_recorder
description: "Simple voice recording app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  record: ^6.1.2
  just_audio: ^0.10.5
  path_provider: ^2.1.5
  path: ^1.9.1
  lottie: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/Lottie/
